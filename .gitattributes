# Normalize line endings: store text as LF in the repo (Vercel builds on Linux).
* text=auto eol=lf

# Binary assets — never diff or convert.
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.avif binary
*.ico  binary
*.woff  binary
*.woff2 binary
